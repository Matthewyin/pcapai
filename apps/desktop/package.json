{
  "name": "@pcapai/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "description": "pcapAI — Agent SDK-first packet troubleshooting workbench",
  "author": "pcapAI",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/preload.cjs dist/preload.cjs",
    "rebuild:native": "npx electron-rebuild -f -w better-sqlite3 --module-dir ../.. --electron-version 33.4.11 && npx electron-rebuild -f -w keytar --module-dir . --electron-version 33.4.11",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build && PCAPAI_DESKTOP_MODE=dev electron .",
    "start": "PCAPAI_DESKTOP_MODE=prod electron .",
    "pack": "npm run rebuild:native && electron-builder --mac dmg --arm64 --x64 --config electron-builder.yml",
    "pack:dir": "npm run rebuild:native && electron-builder --mac dir --config electron-builder.yml"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "electron": "33.4.11",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "keytar": "^7.9.0"
  }
}
